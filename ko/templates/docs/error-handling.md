<!-- Beaver docs 스킬레톤 — analyze 가 채운다. 근거 경로 표기. -->

# Error Handling

## 기본 원칙

<!-- 예외 타입·계층, throw/catch 위치 정책, 응답 메시지 출처(i18n/상수), 1줄 throw 규칙 등.
     특정 LAYER/UNIT만이 아니라 모두에 적용.
     이 프로젝트가 실제로 쓰는 에러 처리 원칙을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 특정 포지션 구문을 가정하지 않는다. 없으면 이 섹션을 비운다. 신규 프로젝트(코드 0)면 감지된 프레임워크의 관용 베이스라인만 적는다. -->

## Repository(데이터 계층) throw 패턴

<!-- 이 헤더는 데이터 접근 LAYER/UNIT 이름이다. 헤더 이름은 그대로 두고 산문·주석으로만 일반화한다.
     통일 문구 사용 이유(범용 메서드는 맥락별 의미가 달라짐 등). DB/데이터 에러 매핑(중복/FK/제약 위반, non-ok fetch 등)을 케이스마다 분기.
     이 프로젝트가 데이터 경계에서 실제로 쓰는 throw 패턴을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 데이터 계층이 없으면 이 섹션을 삭제한다. -->
```
```

## Service(비즈니스 계층) throw 패턴

<!-- 이 헤더는 비즈니스 LAYER/UNIT 이름이다. 헤더 이름은 그대로 두고 산문·주석으로만 일반화한다.
     단순 메시지 vs validationErrors 포함. 예시.
     이 프로젝트가 진입점에서 비즈니스 로직을 실행하며 실제로 쓰는 throw 패턴을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. throw인지 타입 지정 에러 결과 반환인지(반환값이 OUTCOME/인터페이스 계약), 프로젝트가 의존하는 경계 직렬화 제약이 있다면 함께 담는다. 없으면 이 섹션을 삭제한다. -->
```
```

## validationErrors 포함 기준

<!-- 사용자 입력 필드 오류만 포함 / 비즈니스·서버 내부값 오류는 제외 등. 표로. -->

| 케이스 | validationErrors |
|---|---|
| 입력 필드 값 오류 | ✅ |
| 비즈니스 규칙·서버 내부값 오류 | ❌ |

## 응답 래퍼 / 상태코드

<!-- 통일 성공/실패 응답 형태, 상태코드 정책(void→204 등). 이는 OUTCOME / 인터페이스 계약을 기술한다.
     이 프로젝트가 실제로 쓰는 성공/실패 응답 형태와 상태코드·exit·반환 정책을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 섹션을 비운다. -->
