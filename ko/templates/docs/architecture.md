<!-- Beaver docs 스킬레톤 — analyze 가 채운다. 안 쓰는 하위 섹션 삭제. 근거 경로 또는 (표준: <프레임워크>) 표기. -->
<!-- 언어/프레임워크/포지션 무관. 이 프로젝트가 실제로 쓰는 스택을 코드 근거(경로:라인)로 도출해 프로젝트가 쓰는 이름 그대로 문서화한다. 특정 포지션 구문을 가정하지 않는다. 코드가 아직 없는 신규 프로젝트면 감지된 프레임워크의 관용 베이스라인으로 채운다. -->

# Architecture

<!-- 스택 한 줄: 프레임워크 + 버전 + 핵심 의존성 (ORM/DB, 인증, API 문서, data layer, 상태, 스타일링 등) -->
<!-- 이 프로젝트가 실제로 선언한 의존성을 코드 근거(경로:라인)로 도출해 프로젝트가 쓰는 이름 그대로 기록한다. 쓰지 않는 것은 적지 않는다. -->

## Directory Layout

<!-- 디렉터리 트리 + 각 디렉터리 역할 주석. 인프라 vs 도메인 vs 공통 구분, 어느 디렉터리가 라우팅/진입 표면을 갖고/안 갖는지. -->
<!-- 이 프로젝트가 실제로 쓰는 디렉터리 역할과 파일 규약을 코드 근거(경로:라인)로 도출해 프로젝트가 쓰는 이름 그대로 기술한다. -->
```
src/
└── ...
```

## 레이어 경계

<!-- 호출 방향 규칙과 각 레이어가 가질 수 있는/없는 책임. -->
<!-- LAYER/UNIT = 이 코드베이스가 실제로 코드를 나누는 구조적 구분. 실재하는 호출 방향과 경계 규칙을 코드 근거(경로:라인)로 도출해 각 레이어/유닛을 프로젝트가 쓰는 이름 그대로 기록한다. 그런 경계가 없으면 비운다. -->

## 새 도메인(모듈) 최소 파일 구조

<!-- 새 기능 단위를 만들 때 생성할 표준 파일 목록. -->
<!-- 프로젝트의 기존 기능 단위가 실제로 어떻게 구성되는지(코드 근거: 경로:라인)에서 이 목록을 도출해 프로젝트가 쓰는 파일 이름/역할 그대로 쓴다. 일관된 패턴이 없으면 비운다. -->
```
<domain>/
└── ...
```

## 새 도메인 추가 절차

<!-- 표준 구조 생성, 등록/연결, 의존성 바인딩 ... 단계로. -->
<!-- ENTRY POINT = 이 코드베이스의 유닛으로 실행이 진입하는 지점. 프로젝트가 실제로 새 유닛을 어떻게 등록·연결하는지(코드 근거: 경로:라인)에서 추가 절차를 프로젝트가 쓰는 용어 그대로 도출한다. -->

## Key Patterns

<!-- 이 프로젝트가 실제로 쓰는 관용 패턴을 코드베이스의 예시 코드로. 패턴이 없는 하위 섹션은 삭제(delete if absent). 목록에 없지만 프로젝트가 쓰는 패턴은 하위 섹션을 추가. -->

### DI / 의존성 주입
<!-- delete if absent. 의존성을 어떻게 제공하고 어디서 등록하는지를 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Transaction
<!-- delete if absent. 트랜잭션 경계(DATA / AFFECTED STATE)를 선언하는 방식을 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Async / Queue / Scheduler
<!-- delete if absent. 백그라운드/큐잉/스케줄 작업을 등록·실행하는 방식(중복 등록 금지 규칙 포함)을 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Data Layer
<!-- delete if absent. 데이터를 어디서 읽고 쓰는지, DATA / AFFECTED STATE 접근을 어떻게 구조화하는지를 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Entry Points
<!-- delete if absent. 요청/호출이 시스템에 진입하는 방식과 그것이 반환하는 OUTCOME / INTERFACE CONTRACT(입력 검증, 응답/반환 형태, redirect, revalidation 등)를 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Boundary / Environment
<!-- delete if absent. 프로젝트가 강제하는 실행/신뢰 경계(어떤 유닛이 넘을 수 있고, 비밀값·서버 전용 모듈처럼 넘으면 안 되는 것)와 환경/설정 분리 방식을 코드 근거(경로:라인)로, 프로젝트가 쓰는 용어 그대로. -->

### Import Alias
<!-- delete if absent. 프로젝트가 실제로 설정한 경로 별칭 매핑(예: @root/ → src/)을 코드 근거(경로:라인)로. -->
