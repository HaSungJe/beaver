<!-- Beaver docs 스킬레톤 — analyze 가 채운다. DATA 레이어가 없으면 — 즉 상태를 *persist*(ORM/DB) 하지도 않고 *원격 데이터를 fetch* 하지도 않으면 — 이 파일과 본문 링크 삭제.
     "data layer"는 persist OR 원격 데이터 fetch 둘 중 하나라도 하면 존재한다. 이 프로젝트가 데이터를 persist 하거나 fetch 하기 위해 실제로 쓰는 것을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 파일을 삭제한다. 특정 포지션의 구문을 가정하지 않는다.
     data-access 표면이 무거우면 entity.md / repository.md 로 더 쪼개도 됨(샘플 참고). -->

# Data Layer

## Entity / Model 규칙

<!-- 제약 명명 표준(PK/UK/IDX/FK), 컬럼 옵션 규칙, 타임스탬프 처리, 관계(FK) 선언 방식.
     이 섹션은 선언 방식과 무관하게 데이터의 "형태(SHAPE)"에 관한 것. 이 프로젝트가 레코드의 형태를 실제로 선언하는 방식을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 비워 둔다. -->

### 제약 이름 표준
<!-- 이 프로젝트가 실제로 정의하는 DB 제약(근거: 경로:라인)으로 표를 채운다. data layer 에 DB 제약이 없으면 이 표는 비어 있을 수 있음 — 대신 프로젝트가 실제로 쓰는 필드/스키마 명명 규약을 산문으로 기술. -->
| Type | Pattern | Example |
|---|---|---|
| PK |  |  |
| UK |  |  |
| FK |  |  |

### Entity 템플릿
```
// 대표 레코드 1개를 규약대로 작성한 템플릿 —
// 이 프로젝트가 레코드의 형태를 실제로 선언하는 데 쓰는 것(근거: 경로:라인)을, 프로젝트가 쓰는 이름 그대로 사용.
```

## Repository / DAO 규칙

<!-- 범용 메서드 우선(CRUD 통일), where 조건 조립 위치, 타입 규칙, try/catch 의무.
     이것은 Data Access 표면 — 읽기/쓰기를 모으는 곳. 이 프로젝트가 데이터를 읽고/쓰는 데 실제로 쓰는 data-access 함수/모듈을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기술한다 — 소스 URL/키/캐시 설정 위치와 에러 처리 규약(try/catch + throw vs 결과 반환) 포함. 없으면 비워 둔다. -->

### 메서드 템플릿
```
```

## Pagination / 목록 조회 패턴

<!-- 목록 조회 표준 패턴(단일 메서드로 total/count/list 처리 등). 단계별로. 이 프로젝트가 실제로 쓰는 페이징 패턴과 total/count/items 조립 위치를 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 섹션을 삭제한다. -->

## 조회 DTO 생성자 규칙

<!-- query 파라미터 타입 변환·기본값 처리 위치, 생성자 시그니처 규칙. 이 프로젝트가 query 파라미터를 실제로 파싱/검증/coerce 하고 기본값을 적용하는 위치를 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 섹션을 삭제한다. -->

## Transaction

<!-- 트랜잭션 경계와 적용 방식 (Architecture 와 중복되면 한쪽만).
     mutation 의 일관성/원자성 경계. 이 프로젝트가 mutation 이후 일관성을 다시 맞추는 방식 — 경계와 rollback/revalidate 전략 — 을 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 섹션을 삭제한다. -->
