<!-- Beaver docs 스킬레톤 — analyze 가 채운다. 안 쓰는 하위 섹션 삭제. 근거 경로 표기. -->
<!-- 아래 규칙은 모두 코드 근거(경로:라인)로 도출해 프로젝트가 쓰는 이름 그대로 기록한다. 특정 포지션 구문을 가정하지 않으며, 해당 유닛이 없는 하위 섹션은 비운다. -->

# Conventions

## Naming

<!-- LAYER/UNIT 산출물 명명 규칙. 충돌 안 나는 명명 공식을 예시로, 이 스택에 실제 존재하는 유닛만 다룬다. -->

| 대상 | 규칙 | 예시 |
|---|---|---|
|  |  |  |

## 파일 / 디렉터리

<!-- 파일명 규칙(<domain>-<feature>.xxx 등)과 기능당 파일 1개 같은 응집 규칙. -->

## 라우트

<!-- ENTRY POINT 경로 규칙. 이 스택이 라우트를 노출하는 방식에 맞춘다. path/query param 표기는 코드베이스 전체에서 통일한다. 라우트가 없으면 이 섹션은 삭제한다. -->

## 핸들러 입력 파라미터

<!-- ENTRY POINT 입력 규칙: 타입 지정(any 금지), 경계 검증, 파라미터 한 줄. 코드베이스 예시 1개 포함. -->
