<!-- Beaver docs 스킬레톤 — analyze 가 채운다. 이 파일은 INTERFACE CONTRACT(진입점 + 그 입출력 계약)를 문서화하며, 언어·프레임워크·포지션 무관이다 — 특정 포지션 구문을 가정하지 않는다. 아래 모든 규약은 이 프로젝트가 실제로 쓰는 것을 코드 근거(경로:라인)로 도출해 기록하고, 프로젝트가 쓰는 이름 그대로 쓴다. 별도의 API 문서화 표면이 없으면 이 파일과 본문 링크 삭제. -->

# API & Docs

## 문서 데코레이터 / 어노테이션 순서

<!-- 각 진입점의 interface contract 를 어떻게 선언/문서화하고 어떤 순서로 두는가. 반환 가능한 모든 에러·실패 outcome 을 정의해야 한다. 이 프로젝트가 계약을 선언/문서화하는 데 실제로 쓰는 방식을 코드 근거(경로:라인)로 도출해 프로젝트가 쓰는 이름 그대로 기록하고, 감지된 프레임워크의 관용 베이스라인을 따른다. 없으면 비우거나 이 섹션 삭제. -->
```
```

## import / 공통 응답 타입

<!-- 진입점 간 재사용하는 공통 성공/실패 계약 형태와 import 경로. 이 프로젝트가 실제로 재사용하는 공통 계약 형태와 그 import 경로를 코드 근거(경로:라인)로 프로젝트가 쓰는 이름 그대로 기록한다. 형태의 노출이 달라지는 경계를 넘으면 그 경계를 명시한다. 없으면 비운다. -->

## 메서드 주석(JSDoc/Javadoc)

<!-- 어느 레이어/유닛의 메서드·함수에 주석 필수인지, 형식. 이 프로젝트가 실제로 주석을 요구하는 레이어/유닛과 그 형식을 코드 근거(경로:라인)로 프로젝트가 쓰는 이름 그대로 기록한다. 그런 규약이 없으면 비운다. -->
```
```

## DTO 구성 / 네이밍

<!-- 계약을 구성하는 데이터 형태 유닛의 규약: 기능당 파일 1개, 이름 충돌 방지, 각 suffix 역할. 이 프로젝트가 실제로 따르는 데이터 형태 유닛 규약 — 기능당 파일 구성, 충돌 방지, 각 suffix 역할 — 을 코드 근거(경로:라인)로 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 표를 비우거나 이 섹션 삭제. -->

| suffix | 용도 | 예시 |
|---|---|---|
|  |  |  |

## 공통 헤더

<!-- 모든 진입점에 붙는 공통 헤더 / 횡단 계약 규칙(다국어 헤더 등). 이 프로젝트가 진입점 전반에 실제로 적용하는 횡단 계약 규칙을 코드 근거(경로:라인)로 프로젝트가 쓰는 이름 그대로 기록한다. 없으면 이 섹션 삭제. -->
