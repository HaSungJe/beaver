---
# 기능 한 줄 요약 (예: "관리자용 회원가입")
feature_goal: ""

# 도메인/모듈명 (프로젝트 디렉터리 구조는 CLAUDE.md 참고)
domain: ""

# 진입점 메서드/엔드포인트. 이 프로젝트가 실제로 쓰는 진입점 식별자를 코드 근거(경로:라인)로
#   도출해, 프로젝트가 쓰는 이름 그대로 기록한다. 특정 포지션 구문을 가정하지 않는다.
#   (키 이름 "api_method"는 그대로 유지)
api_method: ""

# 경로/시그니처 (예: "/api/v1/user/admin/sign")
#   이 프로젝트가 실제로 쓰는 경로/시그니처를 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는
#   이름 그대로 기록한다. (키 이름 "api_path"는 그대로 유지)
api_path: ""

# 영향받는 상태 — 쓰기/영속화 대상. 테스트 케이스 수 산정 근거. (키 이름 "affected_data"는 그대로 유지)
#   이 프로젝트가 실제로 쓰기/영속화하는 상태를 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는
#   이름 그대로 기록한다. 읽기 전용 읽기는 "쓰기"는 아니나 여전히 데이터. 없으면 비운다.
affected_data: []
---

## 기능 설명
<!-- 이 기능이 왜 필요하고 무엇을 하는지 -->

## API Spec
<!-- 입력(Body/Params/Args), 출력(Response/Return), 형태.
     이 진입점에 대해 이 프로젝트가 실제로 쓰는 계약을 코드 근거(경로:라인)로 도출해,
     프로젝트가 쓰는 이름 그대로 기술한다.
     비-HTTP 진입점이라도 아래 Method/Path/Request/Response 키는 유지
     (Method는 진입점 동사/유형, Path는 시그니처로 재사용). -->
- Method:
- Path:
- Request:
- Response:

## 비즈니스 규칙
<!-- 권한, 중복 체크, 조건 분기 등 -->

## 참고사항
<!-- 관련 도메인, 참고할 기존 코드, 특이사항 -->

## 제안 (코드베이스 기반)
<!-- plan이 연관된 기존 요소를 스캔해 "이런 것도 있으면 좋다 / 기존 X 연계 / Y 패턴 재사용"을
     근거(경로:라인)와 함께 제안. 사용자가 수락/거부.
     이 프로젝트가 실제로 가진 연관 요소를 코드 근거(경로:라인)로 도출해, 프로젝트가 쓰는 이름
     그대로 제시한다. 없으면 섹션 생략.
     예 - [ ] 기존 t_user_log와 연계해 가입 이력 적재 (src/.../user-log.entity.ts:12) -->

## 확정 설계 결정사항
<!-- CLAUDE.md만으로 못 정하는 항목을 - [ ]로 나열(답 비움). 미답 있으면 plan 작성 중단.
     이 프로젝트가 실제로 직면하는 결정포인트만, 코드 근거(경로:라인)와 프로젝트 규약에 기반해
     제기한다.
     예 "- [ ] 인증 필요 여부 — 근거: CLAUDE.md 인증 섹션" -->
