<!-- Beaver docs skeleton — analyze fills this in. Delete unused subsections. Note source paths. -->
<!-- Derive every rule below from code evidence (path:line) and record it with the project's own names. Don't assume any particular position's syntax; omit a subsection if its unit doesn't exist here. -->

# Conventions

## Naming

<!-- Naming rules for LAYER/UNIT artifacts. Provide collision-free naming formulas as examples, covering the units your stack actually has. -->

| Target | Rule | Example |
|---|---|---|
|  |  |  |

## Files / Directories

<!-- File naming rules (e.g. <domain>-<feature>.xxx) and cohesion rules such as one file per feature. -->

## Routes

<!-- ENTRY POINT path rules, adapted to how this stack exposes routes. Unify path/query param notation across the codebase. Omit this section if the project has no routes. -->

## Handler Input Parameters

<!-- ENTRY POINT input rules: typed (no `any`), validated at the boundary, one parameter per line. Include one example from this codebase. -->
