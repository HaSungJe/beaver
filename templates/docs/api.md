<!-- Beaver docs skeleton — filled in by analyze. This file documents the INTERFACE CONTRACT (entry points + their inputs/outputs), language/framework/position agnostic — assume no particular position's syntax. Derive every convention below from what this project actually uses, cited by code evidence (path:line), and use the project's own names. If this project has no separate API-documentation surface to describe, delete this file and the link to it in the body. -->

# API & Docs

## Documentation Decorator / Annotation Order

<!-- How the interface contract of each entry point is declared/documented, and in what order. Every error / failure outcome that can be returned must be defined. Record the mechanism this project actually uses to declare/document a contract — from code evidence (path:line), using the project's own names — and follow its idiomatic baseline for the detected framework. If the project has none, leave this empty or delete the section. -->
```
```

## Imports / Common Response Types

<!-- Common success/failure contract shapes reused across entry points and their import paths. Record the shared contract shapes this project actually reuses and where they are imported from — from code evidence (path:line), using the project's own names. Note any boundary an import path crosses where the shape's exposure changes. If the project has none, leave this empty. -->

## Method Comments (JSDoc/Javadoc)

<!-- Which layer/unit's methods or functions require comments, and the format. Record which layer/unit this project actually requires comments on and the format it uses — from code evidence (path:line), using the project's own names. If the project has no such convention, leave this empty. -->
```
```

## DTO Structure / Naming

<!-- Convention for the data-shape units that make up the contract: one file per feature, how to avoid name collisions, and the role of each suffix. Record the data-shape-unit convention this project actually follows — per-feature file organization, collision avoidance, and each suffix's role — from code evidence (path:line), using the project's own names. If the project has none, leave the table empty or delete the section. -->

| suffix | purpose | example |
|---|---|---|
|  |  |  |

## Common Headers

<!-- Cross-cutting contract rules applied across all entry points, such as localization headers. Record the cross-cutting contract rules this project actually applies — from code evidence (path:line), using the project's own names. If the project has none, delete this section. -->
