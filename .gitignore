# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Beaver runtime state (only relevant if this repo is itself managed by beaver)
.beaver/.retry-count
.beaver/.current-spec
.beaver/.auto-branch-state.json
.beaver/.dam-state.json
